name: Run test on push

on:
  push:
    branches:
      - devel
      - master
      - env_config
jobs:
  run_tests:
    runs-on: [ubuntu-latest]

    env:
      APP_DEBUG: y
      ALLOWED_HOSTS: '*'
      DEFAULT_EMAIL: nerosketch@gmail.com
      ADMINS: '[["Dmitry Novikov", "nerosketch@gmail.com"]]'
      POSTGRES_DB: djing2
      POSTGRES_USER: djing2usr
      POSTGRES_HOST: postgres
      API_AUTH_SUBNET: '127.0.0.0/8'
      MESSENGER_BOT_PUBLIC_URL: 'http://localhost'
#     ARPING_ENABLED: y
      ARPING_COMMAND: echo
      SORM_EXPORT_FTP_HOST: localhost
      SORM_EXPORT_FTP_USERNAME: cdr
      SORM_EXPORT_FTP_DISABLE: y
      RADIUS_APP_HOST: localhost

    steps:
        # Чекаутим код
      - uses: actions/checkout@master
      - name: Make secrets
        run: base64 /dev/urandom | head -c20 > ./secrets/API_AUTH_SECRET \
          && base64 /dev/urandom | head -c50 > ./secrets/DJANGO_SECRET_KEY \
          && base64 /dev/urandom | head -c32 > ./secrets/FIELD_ENCRYPTION_KEY \
          && base64 /dev/urandom | head -c20 > ./secrets/RADIUS_APP_SECRET \
          && base64 /dev/urandom | head -c20 > ./secrets/SORM_EXPORT_FTP_PASSWORD \
          && base64 /dev/urandom | head -c20 > ./secrets/VAPID_PUBLIC_KEY \
          && base64 /dev/urandom | head -c20 > ./secrets/VAPID_PRIVATE_KEY \
          && base64 /dev/urandom | head -c20 > ./secrets/POSTGRES_PASSWORD \
          && base64 /dev/urandom | head -c20 > ./secrets/RADIUS_SECRET
      - name: Build docker containers
        run: docker-compose build
      - name: Run tests
        run: docker exec djing2_app ./manage.py test --noinput
